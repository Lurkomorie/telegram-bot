# Telegram Bot
BOT_TOKEN=your_telegram_bot_token_here
WEBHOOK_SECRET=generate_random_secret_here
PUBLIC_BASE_URL=https://your-app.railway.app

# Database (Railway PostgreSQL - separate from main app)
DATABASE_URL=postgresql://postgres:your_password@your_host:port/railway

# Test Database (Optional - for testing without affecting production data)
# Create a separate PostgreSQL database on Railway and set this URL
# TEST_DATABASE_URL=postgresql://postgres:your_password@your_test_host:port/railway
# USE_TEST_DB=false  # Set to true to use test database instead of main database

# Redis (using your existing Upstash Redis)
REDIS_URL=redis://default:your_redis_password@your_redis_host:6379

# AI Services
OPENROUTER_API_KEY=your_openrouter_api_key_here
RUNPOD_API_KEY_POD=your_runpod_api_key_here
RUNPOD_ENDPOINT=https://your_runpod_endpoint.proxy.runpod.net/run

# Security
IMAGE_CALLBACK_SECRET=your_callback_secret_here

# Cloudflare (for analytics image storage)
CLOUDFLARE_API_TOKEN=I-K5x0KQqaU0XYjY4y5R-npuwfvwy3mZmqhQQJEu
CLOUDFLARE_ACCOUNT_ID=63960ca6e87fe07b032de4970fdebdef
CLOUDFLARE_ACCOUNT_HASH=Gi73yIeWSwM8-OnKb7CLUA

# Admin Panel
ADMIN_USERNAME=admin
ADMIN_PASSWORD=change_this_secure_password
ADMIN_SECRET_KEY=generate_random_secret_key_here

# Environment
ENV=production
LOG_LEVEL=INFO

# Logging Mode (for local development)
# Set to "development", "dev", or "local" for verbose logging
# Set to "production" or omit for minimal logging (Railway-friendly)
ENVIRONMENT=production

# Feature Flags (optional, defaults to True for bot/followups, False for images in followup)
# Set ENABLE_BOT=False to disable bot (useful for testing APIs locally without bot responding)
# Set ENABLE_FOLLOWUPS=False to disable auto follow-up messages
# Set ENABLE_IMAGES_IN_FOLLOWUP=True to enable image generation during auto follow-ups (disabled by default to save resources)
# Set SERVICE_UNAVAILABLE=True to show maintenance message to users (only shows once per user)
# Set FOLLOWUP_TEST_USERS to restrict followups to specific user IDs for testing (comma-separated, e.g., "123456,789012")
# Set SIMULATE_PAYMENTS=True to simulate successful payments in development without real Telegram Stars charges
ENABLE_BOT=True
ENABLE_FOLLOWUPS=True
ENABLE_IMAGES_IN_FOLLOWUP=False
SERVICE_UNAVAILABLE=False
SIMULATE_PAYMENTS=False
# FOLLOWUP_TEST_USERS=


